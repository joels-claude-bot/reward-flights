# Dev log — 2026-06

## 2026-06-26 18:10 — Scaffold MDX docs site + Amex UK reward-flights content
Context: Greenfield repo. Goal: a guide for maximising Amex UK Membership Rewards (MR) points on reward flights, with an interactive MDX docs site.
Action:
- Scaffolded Rspress (MDX engine, per ADR-008 / mdx-documentation skill) at repo root.
- Port block computed: base = 9000 + crc32("reward-flights") % 900 = **9307** (crc32 verified against test vector cbf43926). Registered `docs = 9307` in `.registered-ports.toml`.
- Added `justfile` (`just docs` runs `rspress dev --port 9307` with a preflight port check; `build`, `preview`).
- Content pages: index, transfer-partners (the sanitised Amex airline table), best-value-airlines, expected-value, when-to-book, finding-flights, destinations.
- Interactive components (the reason for MDX): `TransferPartnersTable.tsx` (sortable/filterable) + `PointValueCalculator.tsx` (pence-per-point EV calc). Shared data in `components/partners.ts` so ratios never drift.
Why: User asked for the airline transfer table sanitised onto its own page, plus a practical playbook (best value, EV £/point, timing, aggregators, destinations) so Holdaisy can book reward flights with minimal hassle.
Result: `rspress build` passes (7 pages, no dead links). Dev server up on :9307 in tmux window `reward-flights:docs`. Headless-Chromium e2e: **9/9 checks pass** (table renders 11 airlines, filter works, calculator recomputes, no console errors). Screenshots in `scripts/`.

Decisions logged:
- Assigned the docs service to the base port (9307); this is a docs-only project with no api/frontend split.
- Used system Chromium (`/home/claude/.nix-profile/bin/chromium`) for Playwright — the cached pw browser build (1228) didn't match installed (1217/1223).
- Content figures (ratios from the supplied Amex table; value tiers, aggregator prices, sweet-spot routes from general reward-travel knowledge) are indicative — flagged as such on every page. NOT independently re-verified against live Amex/airline sites.

Parked / not built: the actual flight-search tooling/app — this task was documentation only.

## 2026-06-26 18:35 — Review passes (code + docs)
Context: Ran the mandated /review-code and /review-docs after building.
Action:
- Tooling note: `gemini` CLI needs GEMINI_API_KEY (absent) so the architectural/structure sweeps were skipped; `llm -m gemini-2.5-flash/pro` works (own key). `llm -a` mis-sniffs .ts/.tsx/.mjs as python — worked around with `--at <file> text/plain` on `.txt` copies. Repo isn't git yet, so reviews ran directly on the new files, not a diff.
- **Code review:** partners.ts + both React components clean. Non-actionable flags: "no runtime validation of static AIRLINE_PARTNERS" (false positive — TypeScript-typed internal constant); `scripts/e2e.mjs` style nits (uses `waitForTimeout` and a custom `ok()` reporter rather than Playwright `expect`) — acceptable for a throwaway verification harness that passed 9/9.
- **Docs review:** visual pass (gemini-2.5-pro vision on full-page screenshots) **clean on all pages** — nothing truncated, all renders, legible, colour + hierarchy good. Content pass flagged acronyms/jargon used before expansion.
Why: Acronym-on-first-use is a standing style rule; worth fixing. The "assumed domain term" flags come from the reviewer persona having zero travel knowledge — addressed centrally rather than bloating every page.
Result: Expanded the index glossary (Amex, award space, redemption, surcharges, sweet spot, cabin) and fixed first-use of Amex / BST / ANA / EV across transfer-partners, best-value-airlines, when-to-book, destinations. Rebuilt (passes, no dead links); e2e re-run **9/9**. One review cycle; not looping further (remaining flags are inherent-domain-term, now glossary-covered).

## 2026-06-28 11:30 — Reader-first restructure + award-search matrix + maps (implementing Codex review + user asks)
Context: User had a Codex session review the docs, then asked me to implement its findings plus their own: concrete-examples-first, 10s/30s takeaways, diagrams, fix the when-to-book wall of text, an award-search compatibility table, and an interactive destinations map. User pointed me to the **llm-site-reviewer lessons-learned** (`~/llm-site-reviewer/docs-site/docs/concept/lessons-learned.md`) — applied throughout.
Action:
- **Lessons applied:** goal → concrete artifact → findings → mechanics; lead with a *why* diagram; categories/tables come AFTER the example; What/Why are drafting notes not headings; pass the 10s/30s skim test.
- **Diagrams:** installed `rspress-plugin-mermaid`, added to config. Home now opens with a worked £450-vs-£2,700 example + a 5-step decision-flow mermaid diagram. when-to-book opens with a booking-window timeline diagram (replacing the wall of text).
- **index.md:** dropped `pageType: home` — that layout *discards the markdown body*, so the worked example/diagram/glossary were invisible. Converted to a normal concrete-first page.
- **Award-search matrix:** new `components/searchTools.ts` + `AwardSearchMatrix.tsx`. finding-flights now leads with a compatibility table (seats.aero / AwardTool / SeatSpy / PointsYeah × the 11 partners), ✓/—/? colour-coded, links + `last checked 2026-06-28`. Coverage verified via WebSearch/WebFetch against each tool's published list (seats.aero=25 progs, SeatSpy=BA/Iberia/KLM/Virgin/Etihad/JetBlue, AwardTool broadest, PointsYeah 22+). Honest `?` for unconfirmed cells.
- **Interactive destinations map:** `components/destinationRoutes.ts` + `DestinationMap.tsx` — airline selector → Great Circle Mapper embedded route image (with graceful onError fallback) + links out to gcmap interactive / FlightConnections / FlightsFrom.
- **Codex fixes:** transfer-partners "Four partners" → "Three" (Aer Lingus is Avios but not a direct Amex partner); heuristic-framing callouts on best-value + destinations; jargon expansions (oneworld, ANA, Delta One, Qantas Frequent Flyer).
Why: The first build was correctly structured in the wrong order (categories before the reader saw a concrete example). The lessons fix exactly that.
Result: Build clean (no dead links). Extended e2e **16/16** (mermaid renders on home + when-to-book; matrix has 4 tools × 11 rows; map switches airlines; calculator still interactive; no console errors). Vision review (gemini-2.5-pro) of all 4 changed pages: CONCRETE_FIRST yes, hierarchy clear, legible.
Caveats: (1) gcmap map image can't be render-verified here — this box has no outbound network (HTTP 000); it loads in a real browser, and the component falls back gracefully if not. (2) Tool coverage/pricing change often — matrix is date-stamped and the `?` cells link out to verify.

## 2026-06-28 12:15 — Summary table readability + per-tool comparison tables + appendix
Context: User asked to (a) simplify the transfer-partners ratio to "1:1"/"2:1" with a green/amber/red indicator, (b) add seats.aero + SeatSpy comparison tables for the Amex UK partners, (c) appendix for non-Amex airlines those tools cover. Provided real status data.
Action:
- `partners.ts`: added `simplifiedRatio()` (gcd-reduced, strips 1000:1000 → 1:1) and `ratioRating()` (good ≥1:1 / ok >0.5 / bad ≤0.5 i.e. 2:1). TransferPartnersTable now has a **Rate** column (coloured dot + simplified ratio) + a **Currency** column + legend; dropped the verbose "1000 MR : 1000 …" string.
- New `toolStatus.ts` with the user-supplied seats.aero per-programme status (healthy/degraded/outage, live-search, beta) and SeatSpy's tracked-airline list, each mapped to its Amex UK partner (or null).
- New `SeatsAeroTable.tsx` + `SeatSpyTable.tsx` (scope `amex` | `other`). finding-flights gained "seats.aero status by Amex UK partner" and "SeatSpy coverage by Amex UK partner" sections; new `appendix.mdx` lists the non-Amex airlines each tool covers (United, American, Etihad, JetBlue, …).
- **Data correction:** the user's SeatSpy list shows it tracks **Cathay Pacific** — fixed the AwardSearchMatrix cell (was "no" → "yes"). SeatSpy Amex coverage is now BA, Cathay, Flying Blue, Iberia, Virgin (5).
Why: the 1000:1000 rows were hard to scan; the traffic light + simplified ratio make good/poor obvious at a glance. Per-tool tables answer "is the airline I want up, and on which tool?".
Result: build clean (appendix added, no dead links); e2e **25/25**; vision review of the 3 changed pages — RENDERS/LEGIBLE OK, COLOUR_KEY CLEAR.

## 2026-06-28 12:50 — PointsYeah authoritative coverage (corrections) + table/appendix
Context: User supplied PointsYeah's actual supported-programmes list (18 programmes).
Action:
- **Matrix corrections** in `searchTools.ts` — PointsYeah was wrong for 4 partners: Emirates yes→no, Singapore yes→no, Cathay check→no, SAS check→no. PointsYeah's Amex UK coverage is now exactly the **popular five**: Flying Blue, Delta, Qantas, Qatar (Avios), Virgin Atlantic. (No more `?` cells for PointsYeah.)
- Added `POINTSYEAH` data to `toolStatus.ts` (18 programmes, mapped to partner-or-null) and `PointsYeahTable.tsx` (mirrors SeatSpyTable — tracked list, no per-airline status). finding-flights gained a "PointsYeah — coverage" section; appendix gained "PointsYeah — other programmes" (13 non-Amex).
- Rewrote the finding-flights 30-second version + "what the matrix tells you" for accuracy: seats.aero has the widest *free* coverage (8/11, incl. Emirates/Singapore/SAS that PointsYeah lacks); PointsYeah overlaps on the popular five.
Why: the earlier PointsYeah cells were my best-guess with `?` placeholders; the user's list makes them authoritative and fixes two outright errors (Emirates, Singapore).
Result: build clean; e2e **29/29** (incl. PointsYeah Qatar tracked, Emirates/Singapore NOT tracked); PointsYeah table + appendix verified by screenshot.
